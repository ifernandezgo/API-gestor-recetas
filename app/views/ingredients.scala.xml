@(ids: List[Integer], ingredients: Map[Integer, String])
<?xml version="1.0" encoding="utf-8" ?>
<ingredients>
    @for(id <- ids) {
        @ingredient_template(id, ingredients.get(id))
    }
</ingredients>